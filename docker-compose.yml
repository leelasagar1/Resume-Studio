services:
  resume-studio:
    build: .
    image: resume-studio
    env_file: .env
    ports:
      # Host port is configurable: PORT=9000 docker compose up
      - "127.0.0.1:${PORT:-8765}:8765"
    restart: unless-stopped
